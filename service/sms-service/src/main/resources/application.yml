spring:
  profiles: default
  application:
    name: sms-service
  cloud:
    config:
      profile: dev
      uri: http://localhost:8888/
      fail-fast: true

server:
  port: 1011
logging:
  level: debug
