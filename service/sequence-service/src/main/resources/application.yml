spring:
  profiles: default
  application:
    name: sequence-service
  cloud:
    config:
      profile: dev
      uri: http://localhost:8888/
      fail-fast: true

server:
  port: 1010
logging:
  level: debug
