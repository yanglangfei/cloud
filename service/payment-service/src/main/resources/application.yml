spring:
  profiles: default
  application:
    name: payment-service
  cloud:
    config:
      profile: dev
      uri: http://localhost:8888/
      fail-fast: true

server:
  port: 1007
logging:
  level: debug
