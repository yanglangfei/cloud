spring:
  profiles: default
  application:
    name: resources-service
  cloud:
    config:
      profile: dev
      uri: http://localhost:8888/
      fail-fast: true

server:
  port: 1009
logging:
  level: debug
